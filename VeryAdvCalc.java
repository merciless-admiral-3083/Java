public class VeryAdvCalc extends AdvCalc//4th file //veryadvcalc extends to advcalc and adv calc xtends to calc, so we are just gathering data at a place before giving it to the main file
{
    public double power(int n1, int n2) //⚠️ALWAYS USE DOUBLE WHEN USING POWER CONCEPT⚠️
    {
        return Math.pow(n1, n2);  //nothing we just added another thing
    }    
}
